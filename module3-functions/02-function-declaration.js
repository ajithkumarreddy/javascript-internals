/**
 * Function declaration
 * Function Declaration is a named function defined using the function keyword and is fully hoisted,
 * allowing it to be called before its declaration in the code.
 *
 * - Function declarations are fully hoisted
 * - Function declarations are stored in memory with full body during creation phase
 */

function functionName(parameters) {
  // code
}
