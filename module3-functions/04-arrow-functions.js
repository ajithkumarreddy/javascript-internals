/**
 * Arrow functions
 * Arrow functions were introduced in ES6 (2015) as a shorter way to write functions.
 */

const add = (a, b) => {
  return a + b; // Explicit return
};

/**
 * Implicit return
 */

const add = (a, b) => a + b;
